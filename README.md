IMPORTANT BEFORE EXECUTING THE INSTALL SCRIPT:
-DISABLE "Secure boot" in the BIOS
-DISABLE "HVCI" (memory integrity protection) in Windows (Parameters> Confidentiality and Security> )
-if required, the password for signing the SBIGTest.pfx certificate is 'test123'.
